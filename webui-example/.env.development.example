# 本地开发建议配置：
# - Vite dev server: http://localhost:3000
# - 后端 API:       http://localhost:8000
# 使用相对路径 + vite proxy，避免 CORS 复杂度。
VITE_API_URL=/api/v1

# 本地联调建议开启 telemetry，方便快速排障。
VITE_ENABLE_DEBUG_LOGS=true

# 本地慢请求阈值可以更严格一些，尽早暴露性能问题。
VITE_SLOW_API_WARN_MS=800
